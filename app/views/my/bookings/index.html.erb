<div class="container">
  <% @bookings.each do |booking| %>
    <p><%= booking.date %></p>
    <%= link_to my_booking_path(booking), method: :delete, data: { confirm: 'Are you sure?' } do %>
      <i class="far fa-trash-alt mr-3"></i>
    <% end %>
  <% end %>
</div>
